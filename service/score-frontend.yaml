apiVersion: score.dev/v1b1
metadata:
  name: frontend
containers:
  main:
    image: ghcr.io/stefanprodan/podinfo:latest
    variables:
      BACKEND_SVC: ${resources.backend.name}
resources:
  backend:
    type: service